{% trans_default_domain 'FOSUserBundle' %}
{{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'form-control', 'style': 'margin-bottom:15px' } }) }}
{{ form_widget(form) }}
<div>
            <input style="margin-bottom:15px" class="form-control btn btn-primary" type="submit" value="{{ 'change_password.submit'|trans }}" />
</div>
{{ form_end(form) }}
